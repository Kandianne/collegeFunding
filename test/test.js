//before you run any tests, wipe out the database

var should = require("should");

describe("Kandi Functions", function() {
	it("should do this + that", function(done) {
		should.equal(3+3, 6);
		done();
	})
})